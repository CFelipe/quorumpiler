if

if, ifelse

if, ifelse, else
if number eq number id eq number elseif number leeq number id eq number else id eq number end
if number eq number id eq number elseif number leeq number id eq number else id eq number end
